# Artifact Registry Entry - BudgetLLM Library Module
# Source: Codebase Survey 2025-12-29

artifact_id: AOS-LIB-BLL-BRD-001
name: backends/redis.py
type: library
status: active
owner: backend
purpose: Redis-backed cache for multi-process state sharing
authority_level: observe
execution_surface: server
traceability:
  product: system-wide
  console: internal
  domain: budgetllm
  subdomain: backends
  order_depth: O3
responsibility: |
  - Shared cache across processes/workers
  - RedisBackend class with:
    - Automatic TTL expiration
    - Atomic operations
    - Configurable key prefix (budgetllm:cache)
    - get, set, delete methods
dependencies:
  - redis
notes: |
  Location: budgetllm/core/backends/redis.py
  Optional dependency: pip install budgetllm[redis]
