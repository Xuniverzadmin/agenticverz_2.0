# Artifact Registry Entry - Operations Script
# Source: Codebase Survey 2025-12-29

artifact_id: AOS-OP-VRF-TIS-001
name: tenant_isolation_test.py
type: script
status: active
owner: ops
purpose: Tenant isolation verification for Phase A.5
authority_level: observe
execution_surface: cli
traceability:
  product: system-wide
  console: internal
  domain: verification
  subdomain: isolation
  order_depth: O2
responsibility: |
  - Tests tenant isolation guarantees:
    - Cost records only visible to owning tenant
    - Advisories only visible to owning tenant
    - Worker runs only visible to owning tenant
dependencies: []
notes: |
  Location: scripts/verification/tenant_isolation_test.py
  Phase: A.5
