# Lightweight Flask webhook receiver for development
FROM python:3.12-slim

WORKDIR /app

# Install Flask
RUN pip install --no-cache-dir flask

# Copy app
COPY app.py .

# Create data directory
RUN mkdir -p /data

EXPOSE 5000

CMD ["python", "app.py"]
