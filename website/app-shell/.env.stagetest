# AOS Console – Stagetest Environment
# Auth bypassed: all routes accessible without login
# For evidence browsing and API verification only

VITE_API_BASE=
VITE_AUTH_BYPASS=true
VITE_ENVIRONMENT=stagetest
VITE_APP_NAME=Agenticverz AOS Console (Stagetest)

# Clerk key required for ClerkProvider (child components use hooks),
# but route guards bypass auth check when VITE_AUTH_BYPASS=true
VITE_CLERK_PUBLISHABLE_KEY=pk_test_c3VpdGFibGUtcXVhaWwtNjguY2xlcmsuYWNjb3VudHMuZGV2JA
