# AOS Console – Preflight Environment
# For testing L2.1 UI projection pipeline output before promotion

# Empty for same-origin requests (subdomain deployment)
VITE_API_BASE=
VITE_API_TIMEOUT=15000
VITE_SSE_URL=/api/v1/events
VITE_AUTH_LOGIN_URL=/login
VITE_APP_NAME=Agenticverz AOS Console (Preflight)

# Preflight indicator
VITE_ENVIRONMENT=preflight
VITE_PREFLIGHT_MODE=true

# UI Projection endpoint
VITE_PROJECTION_URL=/projection/ui_projection_lock.json
